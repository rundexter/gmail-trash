/*
 * Metadata describing the module operation
 */
{
    //Required. Inputs the module expects.
    "inputs": [
      {
        "id": "id",
        "title":"The ID of the message to delete"
      },
      {
        "id": "userId",
        "title":"The user's email address"
      }
    ],
    //Required. Outputs the module emits.
    "outputs": [
        {
            "id": "isRemoved",
            "title":"isRemoved"
        }
    ],
    "title": "Google Email Delete" /* Required.A display name for the module */
}
